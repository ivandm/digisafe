window.onload = (event) => {
    console.log('page is fully loaded');
    var obj = $('.autocompleteselect');
    console.log(obj);
};
function getOption(obj) {
    let $ = django.jQuery;
    var obj = $('.autocompleteselect');
    console.log(obj);
};